apiVersion: apps/v1
kind: Deployment
metadata:
  name: iris-service
  namespace: ml-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iris-service
  template:
    metadata:
      labels:
        app: iris-service
    spec:
      containers:
        - name: iris-service
          # Replace with the image you pushed: docker push <username>/iris-service:v1
          image: REPLACE_WITH_YOUR_DOCKERHUB_USERNAME/iris-service:v1
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: iris-service
  namespace: ml-demo
spec:
  selector:
    app: iris-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
