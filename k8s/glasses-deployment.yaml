apiVersion: apps/v1
kind: Deployment
metadata:
  name: glasses-service
  namespace: ml-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: glasses-service
  template:
    metadata:
      labels:
        app: glasses-service
    spec:
      containers:
        - name: glasses-service
          # Replace with the image you pushed: docker push <username>/glasses-service:v1
          image: REPLACE_WITH_YOUR_DOCKERHUB_USERNAME/glasses-service:v1
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: glasses-service
  namespace: ml-demo
spec:
  selector:
    app: glasses-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
