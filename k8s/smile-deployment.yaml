apiVersion: apps/v1
kind: Deployment
metadata:
  name: smile-service
  namespace: ml-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smile-service
  template:
    metadata:
      labels:
        app: smile-service
    spec:
      containers:
        - name: smile-service
          # Replace with the image you pushed: docker push <username>/smile-service:v1
          image: REPLACE_WITH_YOUR_DOCKERHUB_USERNAME/smile-service:v1
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: smile-service
  namespace: ml-demo
spec:
  selector:
    app: smile-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
