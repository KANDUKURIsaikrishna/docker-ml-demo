apiVersion: apps/v1
kind: Deployment
metadata:
  name: diabetes-service
  namespace: ml-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diabetes-service
  template:
    metadata:
      labels:
        app: diabetes-service
    spec:
      containers:
        - name: diabetes-service
          # Replace with the image you pushed: docker push <username>/diabetes-service:v1
          image: REPLACE_WITH_YOUR_DOCKERHUB_USERNAME/diabetes-service:v1
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: diabetes-service
  namespace: ml-demo
spec:
  selector:
    app: diabetes-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
